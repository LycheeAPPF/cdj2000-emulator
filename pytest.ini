[pytest]
# The tools are imported as `tools.cdj_gui...`, which only resolves when the
# repository root is on sys.path.  `python -m pytest` puts it there and a bare
# `pytest` does not, so without this line half the ways of running the suite
# fail with ModuleNotFoundError before a single test runs.
pythonpath = .
testpaths = tests
